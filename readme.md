A data table component in Vue2
For each row of data, the column is an input field for a form, when a user updates a field, console logging.
The “rating” field is dropdown input

yarn cypress open-ct

or 

yarn dev

